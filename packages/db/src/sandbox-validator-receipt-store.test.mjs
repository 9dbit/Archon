import test from 'node:test';import assert from 'node:assert/strict';import {createSandboxValidatorReceiptStore} from './sandbox-validator-receipt-store.mjs';
test('receipt persists only beside matching SUBMITTING validator claim',async()=>{let sql;const store=createSandboxValidatorReceiptStore(async(q)=>{sql=q;return [{}]});await store.storePrepared({schemaVersion:1,runId:'run-1',manifestSha256:'a'.repeat(64),artifactEvidenceSha256:'b'.repeat(64),ciphertext:'x',iv:'c'.repeat(24),authTag:'d'.repeat(32),expiresAt:new Date().toISOString()});assert.match(sql,/state='SUBMITTING'/);assert.match(sql,/ON CONFLICT DO NOTHING/);});
