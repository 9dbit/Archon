import test from 'node:test';import assert from 'node:assert/strict';import {sealValidatorReceipt,openValidatorReceipt} from './sandbox-validator-receipt.mjs';
const key='a'.repeat(64),receipt={schemaVersion:1,runId:'run-1',manifestSha256:'b'.repeat(64),artifactEvidenceSha256:'c'.repeat(64),bucketKey:'archon_sandbox_'+'d'.repeat(24),reportKey:'run-1/archon-reopen-report.json',uploadKey:'private',expiresAt:new Date(Date.now()+60000).toISOString()};
test('validator receipt encrypts capability and binds all evidence',()=>{const sealed=sealValidatorReceipt({receipt,secret:key});assert.ok(!JSON.stringify(sealed).includes('private'));assert.equal(openValidatorReceipt({record:sealed,secret:key}).uploadKey,'private');});
test('tampered evidence or key cannot decrypt',()=>{const sealed=sealValidatorReceipt({receipt,secret:key});for(const record of [{...sealed,artifactEvidenceSha256:'e'.repeat(64)},sealed])assert.throws(()=>openValidatorReceipt({record,secret:record===sealed?'f'.repeat(64):key}),/AUTHENTICATION_FAILED/);});
